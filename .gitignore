# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Private notes — never publish (contains personal context, not site content)
CLAUDE.md

# LaTeX build artifacts (keep the .tex, drop the rest)
cv/*.aux
cv/*.log
cv/*.out
cv/*.synctex.gz
cv/*.fls
cv/*.fdb_latexmk

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Browser QA screenshots
.playwright-mcp
